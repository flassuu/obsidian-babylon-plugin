import { Plugin } from 'obsidian';

export default class BabylonPlugin extends Plugin {
	async onload() {
		// Plugin will be built incrementally per SPECIFICATION.md and ROADMAP.md
	}

	onunload() {}
}
